version: "3.9"

services:
  app:
    build:
    # Uses current directory
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    # Maps our local to container
    volumes:
     - ./app:/app
    command: >
     sh -c "python manage.py runserver 0.0.0.0:8000"